import {StyleSheet} from 'react-native';

const viewFriendsToChat = StyleSheet.create({

    loadingViewIndicator:{
        flex:1,
        justifyContent:'center',
        alignItems:'center',
        backgroundColor:'white',
    },

    wrapper:{
        flex:1,
    }
})

export default viewFriendsToChat