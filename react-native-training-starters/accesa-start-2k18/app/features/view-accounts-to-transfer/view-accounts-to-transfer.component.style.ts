import {StyleSheet} from 'react-native';

const viewAccountsToTransferMoneyStyle = StyleSheet.create({

    loadingViewIndicator:{
        flex:1,
        justifyContent:'center',
        alignItems:'center',
        backgroundColor:'white',
    }

})

export default viewAccountsToTransferMoneyStyle;